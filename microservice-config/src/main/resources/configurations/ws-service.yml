server:
  port: 8082

spring:
  application:
    name: ws-service
    version: 0.01

  data:
    mongodb:
      uri: mongodb://localhost:27017/notify

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://localhost:8761/eureka}
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,loggers,httptrace,scheduledtasks,mappings,logfile
        exclude: beans,env,threaddump
  endpoint:
    health:
      show-details: never

info:
  app:
    name: webflux-websockets-service
    description: Sistema spring reactivo webflux
    version: 1.0